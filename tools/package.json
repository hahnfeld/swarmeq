{
  "name": "swarmeq-tools",
  "version": "0.3.1",
  "description": "Maintainer-only build chain for swarmeq. Not shipped to users.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "esbuild": "0.24.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  }
}
